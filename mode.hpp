#ifndef __MODE_HPP__
#define __MODE_HPP__

//#include "mode.hpp"
#include "cmd_parser.hpp"

void CMD_MODE();

void TEST_MODE(std::string ckt_name, int num_test);

void BATCH_MODE(std::string batch_file_name);

#endif